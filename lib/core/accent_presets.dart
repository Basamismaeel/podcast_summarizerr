import 'package:flutter/material.dart';

/// Preset accent colors for [ColorScheme.fromSeed] (Material 3).
abstract final class AccentPresets {
  static const List<Color> colors = [
    Color(0xFF007AFF), // system blue
    Color(0xFF5856D6), // indigo
    Color(0xFFAF52DE), // purple
    Color(0xFFFF2D55), // pink
    Color(0xFFFF3B30), // red
    Color(0xFFFF9500), // orange
    Color(0xFFFFCC00), // yellow
    Color(0xFF34C759), // green
    Color(0xFF30D158), // mint
    Color(0xFF5AC8FA), // cyan
    Color(0xFF64D2FF), // sky
    Color(0xFFA2845E), // brown
  ];
}
